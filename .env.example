# PostgreSQL connection string
# Example (local docker):
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/rental_info
# Example (managed):
# DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DBNAME?sslmode=require
DATABASE_URL=

# Optional: true when your managed DB requires TLS and your URL doesn't include sslmode
DB_SSL=false
